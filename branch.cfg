cfgs:
  default:
    branches: ['master']

  release-branches:
    index: 1
    branches: ['release-.*']
    inherit:
      metrics-exporter:
        jobs:
          release:
            traits:
              release:
                nextversion: 'bump_patch'
